[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: requires network / external API (yfinance, real Slack webhook)
addopts = -m "not integration" -ra --strict-markers
